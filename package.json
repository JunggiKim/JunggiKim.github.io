{
  "name": "junggikim-github-io",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "build:site": "bundle exec jekyll build",
    "test": "npm run build:site && playwright test"
  }
}
